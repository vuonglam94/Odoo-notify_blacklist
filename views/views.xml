<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="notify_blacklist.list">
      <field name="name">notify_blacklist list</field>
      <field name="model">notify_blacklist.notify_blacklist</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="notify_blacklist.action_window">
      <field name="name">notify_blacklist window</field>
      <field name="res_model">notify_blacklist.notify_blacklist</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="notify_blacklist.action_server">
      <field name="name">notify_blacklist server</field>
      <field name="model_id" ref="model_notify_blacklist_notify_blacklist"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="notify_blacklist" id="notify_blacklist.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="notify_blacklist.menu_1" parent="notify_blacklist.menu_root"/>
    <menuitem name="Menu 2" id="notify_blacklist.menu_2" parent="notify_blacklist.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="notify_blacklist.menu_1_list" parent="notify_blacklist.menu_1"
              action="notify_blacklist.action_window"/>
    <menuitem name="Server to list" id="notify_blacklist" parent="notify_blacklist.menu_2"
              action="notify_blacklist.action_server"/>
    -->
  </data>
</odoo>